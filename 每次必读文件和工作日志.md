# ATRI -My Dear Moments- 语音数据集提取项目

**最后更新**: 2025-12-24 19:55  
**当前状态**: ✅ 数据集生成完成，语音持续录制中

---

## 📊 当前成果

| 项目 | 数量 | 状态 |
|------|------|------|
| 📜 剧本文件 (.scn) | 32 个 | ✅ 反编译完成 |
| 📋 文本记录 | 4683 条 | ✅ 提取完成 |
| 🎵 语音文件 (.opus) | 584+ 个 | 🔄 持续录制中 |
| ✨ 匹配数据对 | 566 条 | ✅ 已生成 |

---

## 📁 文件结构

```
H:\GDUT2025_12\Voice_atri_mika\
├── 每次必读文件和工作日志.md     # 本文件
├── dataset.csv                  # 全部文本记录 (4683条)
├── extract_dialogue.py          # 文本提取脚本
├── generate_dataset.py          # 数据集生成脚本
│
├── decrypted\                   # 反编译后的剧本
│   ├── b101.ks.scn             # 原始 SCN 文件
│   ├── b101.ks.json            # 反编译后的 JSON
│   └── ...                     # (共32个章节)
│
├── voices\                      # 已提取的语音文件
│   ├── atr_b101_001.opus       # ATRI 语音
│   ├── min_*.opus              # 美奈子语音
│   ├── ryu_*.opus              # 龙司语音
│   └── ...                     # (持续增加中)
│
├── final_dataset\               # ⭐ 最终数据集
│   ├── dataset_matched.csv     # 语音-文本对齐 CSV
│   └── dataset_matched.json    # 语音-文本对齐 JSON
│
├── FreeMote\                    # 反编译工具
└── KirikiriTools\               # 运行时提取工具
```

---

## 🎮 录制状态

**游戏目录**: `X:\SteamLibrary\steamapps\common\ATRI -My Dear Moments-\`

已部署:
- ✅ `version.dll` - KirikiriTools 运行时提取插件
- ✅ `extract-unencrypted.txt` - 触发文件

**解密文件输出**: `unencrypted\` 文件夹

---

## 📋 使用说明

### 查看当前语音数量
```powershell
(Get-ChildItem "X:\SteamLibrary\steamapps\common\ATRI -My Dear Moments-\unencrypted" -Filter "*.opus" -Recurse).Count
```

### 同步语音到工作目录
```powershell
Copy-Item "X:\SteamLibrary\steamapps\common\ATRI -My Dear Moments-\unencrypted\*.opus" -Destination "H:\GDUT2025_12\Voice_atri_mika\voices\" -Force
```

### 重新生成数据集
```powershell
python "H:\GDUT2025_12\Voice_atri_mika\generate_dataset.py"
```

### 生成数据集并转换音频为 WAV
```powershell
python "H:\GDUT2025_12\Voice_atri_mika\generate_dataset.py" --convert
```

---

## 🔢 角色语音文件命名规则

| 前缀 | 角色 |
|------|------|
| `ATR_` | アトリ (ATRI) |
| `MIN_` | 美奈子 |
| `RYU_` | 龙司 |
| `CAT_` | キャサリン |
| `XYA_` | 其他角色 |

文件名格式: `{角色}_b{章节}{场景}_{序号}.opus`

例: `ATR_b304_133.opus` = ATRI 第3章第4节第133句

---

## ⏭️ 后续工作

1. **继续录制** - 按住 Ctrl 快进，直到语音数量达到 4000+
2. **同步语音** - 定期运行同步命令
3. **重新生成数据集** - 更新匹配结果
4. **音频转换** - 使用 ffmpeg 将 opus 转为 wav
5. **模型训练** - 使用最终数据集训练 TTS/ASR 模型

---

## 📝 备注

- 语音文件在游戏运行时实时提取，不会丢失
- 剧本数据已完整，包含全部 4683 条对话
- 匹配率会随着录制进度增加而提高
